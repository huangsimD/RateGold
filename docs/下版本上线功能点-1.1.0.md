# 下版本上线功能点 — 1.1.0

**目标版本号**：`1.1.0`（versionName） / `+3`（versionCode，Closed testing）  
**相对线上**：Production 现网为 `1.0.0+2`  
**发布策略**：先 Closed testing 验证 → 通过后再议 Production  
**维护约定**：凡对本仓库 **App（`lib/` / `android/`）有功能新增或行为变更**，或新增/调整与下版本上架相关的后台能力，须在本文件「功能点清单」中自动补充一行（含状态）。版本号升级时复制本文件为新版本号文档并清空「已上线」项。

---

## 状态图例

| 状态 | 含义 |
|------|------|
| 规划中 | 已定范围未开工 |
| 开发中 | 代码进行中 |
| 测试中 | 仅 Closed / Internal testing |
| 已上线 | 已进 Production |
| 暂缓 | 本版本不做 |

---

## 功能点清单（1.1.0）

| # | 模块 | 功能点 | 说明 | 状态 |
|---|------|--------|------|------|
| 1 | Ops 后台 | 分析看板 | 总用户、今日新增、今日浏览、DAU/WAU、D1/D7 留存、近 7 日趋势 | 测试中 |
| 2 | Ops 后台 | 用户列表 | 匿名 `install_id`、首末活跃、打开次数、最近屏幕、版本、语言；分页搜索 | 测试中 |
| 3 | Ops 后台 | 事件接收 API | `POST /v1/events`（ingest token）；JSON 文件持久化（`ops/data/ops.json`） | 测试中 |
| 4 | App | 可开关匿名埋点 | `OpsAnalytics`；仅 `OPS_BASE_URL` 非空时上报；正式包默认关闭 | 测试中 |
| 5 | App | 冷启动 / 屏幕浏览事件 | `first_open`、`app_open`、`screen_view`（board/convert/settings/gold 等） | 测试中 |
| 6 | 构建 | Closed testing 专用 AAB 脚本 | `tool/build_ops_test_aab.ps1` → `1.1.0+3` + dart-define | 测试中 |
| 7 | 构建 | 正式包保持无埋点 | `tool/build_release_aab.ps1` 不注入 OPS_* | 测试中 |
| 8 | 文档 | Ops 部署与 Data Safety 门槛 | `docs/fx-gold-board-ops.md` | 测试中 |
| 9 | 合规 | Production 含埋点前更新 Data Safety / 隐私政策 | 测试轨道暂不改商店声明 | 规划中 |
| 10 | 产品 | 多语言 | V1 已具备；**1.1.0 不改** | 暂缓 |
| 11 | 产品 | Pro 订阅 / 提醒 / Widget | PRD V1.1 原路径；本阶段不做 | 暂缓 |
| 12 | Ops 部署 | 正式服后台上线 | Vercel `https://rategold-ops.vercel.app` + GitHub 私有 Gist 持久化；链接写入仓库 README | 已上线 |
| 13 | 仓库 | 代码推送 GitHub | https://github.com/huangsimD/RateGold （README 含正式 Ops 链接） | 已上线 |
| 14 | Ops 后台 | Play 商店页 | 查看 Play 安装/活跃；支持手动 CSV 快照 + 可选 Reporting API；不改 App 包 | 已上线 |

---

## 相关路径

- Ops 服务：`ops/`
- App 埋点：`lib/services/ops_analytics.dart`
- 路由上报：`lib/router/app_router.dart`
- 测试包脚本：`tool/build_ops_test_aab.ps1`
- Ops 说明：`docs/fx-gold-board-ops.md`

---

## 变更日志（按日追加）

### 2026-08-02

- 新增 Ops（Express + JSON 存储 + 管理页）与 App 侧 `OpsAnalytics`（编译期开关）。
- 因本机无法编译 `better-sqlite3`，改为无原生依赖的 JSON 持久化，接口与看板指标不变。
- 新增文档维护约定与 `.cursor/rules/next-release-features.mdc`：App/下版本相关改动须更新本清单。
- Ops 正式服部署至 Vercel：https://rategold-ops.vercel.app ；生产数据写入 GitHub 私有 Gist；仓库 README 公示后台链接。
- 新增 Ops「Play 商店」页：不自动同步 Console；可用手动 CSV 或不改包配置 Reporting API 查看安装/活跃。
