---
description: 版本迭代功能清单必须写入 Version Plan 并随改动同步更新
globs:
  - lib/**/*
  - android/**/*
  - ops/**/*
  - tool/build_*.ps1
  - Version Plan/**/*
  - docs/下版本上线功能点-*.md
alwaysApply: false
---

# Version Plan（版本迭代功能清单）

当本会话修改或新增 **App 功能**（`lib/`、`android/`）、**运营/埋点相关**（`ops/`、`OpsAnalytics`、ops 构建脚本），或改变下版本上架行为时：

1. 打开并更新当前目标版本文档：`Version Plan/vX.Y.Z.md`（例如 `Version Plan/v1.1.0.md`）。
2. 在「功能点清单」中新增或改状态（规划中 / 开发中 / 测试中 / 已上线 / 暂缓）。
3. 在对应「功能描述」补充或修订目标 / 行为 / 验收（若为新功能）。
4. 在「变更日志」按当日追加简短条目。
5. 若开始新的 versionName：新建 `Version Plan/vX.Y.Z.md`，并更新 `Version Plan/README.md` 版本索引。
6. **不要**再往 `docs/下版本上线功能点-*.md` 追加内容（该路径仅保留跳转说明）。

不要把 Production 已上架且无计划变更的能力重复写成新功能点。
