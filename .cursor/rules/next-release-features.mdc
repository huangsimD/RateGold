---
description: 下版本上线功能点文档必须随 App/相关改动同步更新
globs:
  - lib/**/*
  - android/**/*
  - ops/**/*
  - tool/build_*.ps1
  - docs/下版本上线功能点-*.md
alwaysApply: false
---

# 下版本上线功能点

当本会话修改或新增 **App 功能**（`lib/`、`android/`）、**运营/埋点相关**（`ops/`、`OpsAnalytics`、ops 构建脚本），或改变下版本上架行为时：

1. 打开并更新当前目标版本文档：`docs/下版本上线功能点-<version>.md`（例如 `1.1.0` → `docs/下版本上线功能点-1.1.0.md`）。
2. 在「功能点清单」中新增或改状态（规划中 / 开发中 / 测试中 / 已上线 / 暂缓）。
3. 在「变更日志」按当日追加简短条目。
4. 若开始新的 versionName，复制上一份文档为新文件名并调整版本号，不要覆盖已上线版本的历史记录职责。

不要把 Production 已上架且无计划变更的能力重复写成新功能点。
